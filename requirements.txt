# --- Stockout-Sentinel :: Laufzeit-Abhaengigkeiten -------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pandas>=2.2
numpy>=1.26

# Multipart-Parser: wird von FastAPI fuer den CSV-Upload
# (POST /api/v1/analyze-csv, multipart/form-data) benoetigt.
python-multipart>=0.0.9

# --- Test / QA ------------------------------------------------------------
pytest>=8.0
# httpx wird vom FastAPI TestClient fuer die API-Integrationstests benoetigt.
httpx>=0.27

# --- Optional: neuronale Prognose ----------------------------------------
# Der TimesFM-Adapter wird zur Laufzeit dynamisch geladen. Ist das Paket
# nicht installiert, faellt die Engine automatisch und ohne Fehler auf den
# robusten statistischen Schaetzer zurueck.
#   pip install "timesfm[torch]"
