# --- Stockout-Sentinel :: Laufzeit-Abhaengigkeiten -------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pandas>=2.2
numpy>=1.26

# Multipart-Parser: wird von FastAPI fuer den CSV-Upload
# (POST /api/v1/analyze-csv, multipart/form-data) benoetigt.
python-multipart>=0.0.9

# --- Prognose: TimesFM ist das zwingende Hauptmodell -----------------------
# Pflicht-Abhaengigkeiten, nicht optional. Bei FORCE_TIMESFM=true (Standard)
# laedt der Service das echte Google-Checkpoint von Hugging Face und
# verweigert den Start, wenn das nicht gelingt - es gibt keinen Fallback.
#
# Hinweis zur Imagegroesse: 'torch' zieht von PyPI die CUDA-Variante (mehrere
# GB). Fuer reine CPU-Deployments stattdessen die schlanke CPU-Variante
# installieren:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
timesfm>=3.0
torch>=2.2

# --- Weboberflaeche -------------------------------------------------------
# Streamlit bringt Altair mit; fuer den Radar-Graphen wird deshalb keine
# zusaetzliche Plot-Bibliothek benoetigt.
# Mindestversion 1.50: davor kennen st.dataframe/st.altair_chart den
# Parameter 'width' noch nicht (Vorgaenger use_container_width ist entfallen).
streamlit>=1.50

# --- Test / QA ------------------------------------------------------------
pytest>=8.0
# httpx wird vom FastAPI TestClient fuer die API-Integrationstests benoetigt.
httpx>=0.27
