[pytest]
testpaths = tests inbox_copilot
pythonpath = .
addopts = -q --strict-markers
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error::DeprecationWarning:src.*
